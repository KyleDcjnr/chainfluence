const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      colors: {
        'lemon-green': '#ccff33', // Or your exact lemon green hex
      },
    },
  },

};

export default config;
